let selectMode; // 0 : input , 1 : input candidate , 2 : erase
let selectNum;


class Board {
    constructor() {
        this.table = new Array(9);
        for (let i = 0; i < 9; i++) {
            this.table[i] = new Array(9);
        }

        
    }
}